version: '3.9'

services:
  web:
    build: 
      context: .
      dockerfile: ./docker/api/Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - web.env
    expose:
      - 8000
    restart: always